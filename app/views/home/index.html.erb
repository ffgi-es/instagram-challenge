<% if user_signed_in? %>
  <%= link_to 'New post', new_post_path %>
<% end %>

<h1>Posts</h1>

<% @posts.each do |post| %>
  <div>
    <% if post.image.attached? %>
      <img src="<%= url_for(post.image) %>" alt="<%= post.caption %>">
    <% end %>
    <p><%= post.caption %></p>
  </div>
<% end %>
